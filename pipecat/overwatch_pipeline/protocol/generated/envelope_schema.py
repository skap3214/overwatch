# generated by datamodel-codegen:
#   filename:  envelope.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Annotated, Any

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field


class Kind(StrEnum):
    """
    Selects which payload schema to apply.
    """

    harness_event = "harness_event"
    harness_command = "harness_command"
    server_message = "server_message"


class Envelope(BaseModel):
    """
    Top-level wrapper for any wire message. The receiver validates this shape first, then validates `payload` against the schema selected by `kind`.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    protocol_version: Annotated[
        str,
        Field(
            description="MAJOR.MINOR — receivers refuse mismatched majors during the client-ready / bot-ready handshake.",
            pattern="^[0-9]+\\.[0-9]+$",
        ),
    ]
    kind: Annotated[Kind, Field(description="Selects which payload schema to apply.")]
    id: Annotated[str, Field(description="Globally unique message id (e.g. ULID).")]
    timestamp: Annotated[
        AwareDatetime, Field(description="ISO-8601 UTC timestamp from the sender.")
    ]
    session_token: Annotated[
        str | None,
        Field(
            description="Per-session HMAC token. Required only on `harness_command` envelopes (orchestrator → daemon). Daemon-originated `harness_event` envelopes are authenticated end-to-end by the relay's ws/host upgrade and don't carry the per-session token. Phone↔orchestrator `server_message` envelopes flow over the Daily data channel which has its own auth, so they don't carry it either."
        ),
    ] = None
    payload: Annotated[
        Any,
        Field(
            description="The kind-specific payload. Validated separately by the receiver."
        ),
    ]
