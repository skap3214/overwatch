# generated by datamodel-codegen:
#   filename:  harness-command.schema.json

from __future__ import annotations

from typing import Annotated, Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel


class Common(BaseModel):
    correlation_id: Annotated[
        str,
        Field(
            description="Unique ID for this turn. Events emitted while executing this command carry the same correlation_id."
        ),
    ]
    target: Annotated[
        str, Field(description="Harness target, e.g. 'claude-code', 'pi', 'hermes'.")
    ]


class Payload(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    text: str


class SubmitText(Common):
    kind: Literal["submit_text"]
    payload: Payload


class Payload1(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    text: str
    cancels_correlation_id: Annotated[
        str, Field(description="The in-flight turn this command preempts.")
    ]


class SubmitWithSteer(Common):
    kind: Literal["submit_with_steer"]
    payload: Payload1


class Payload2(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    target_correlation_id: Annotated[
        str, Field(description="The in-flight turn to cancel.")
    ]


class Cancel(Common):
    kind: Literal["cancel"]
    payload: Payload2


class HarnessCommand(RootModel[SubmitText | SubmitWithSteer | Cancel]):
    root: Annotated[
        SubmitText | SubmitWithSteer | Cancel,
        Field(
            description="Discriminated union of commands sent from the cloud orchestrator to the Mac session-host daemon. The daemon's command allowlist enforces that only these three kinds are accepted — anything else is rejected and audit-logged. Only user-initiated input (voice or typed) ever produces a submit_with_steer or cancel; background events route through the registry instead.",
            title="HarnessCommand",
        ),
    ]
